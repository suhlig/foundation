# suhlig.foundation.main

Ansible role to configure things I want everywhere as a foundation.

Expects that the non-root user already exists and is used for the ssh connection.

become: true is used only where needed.
