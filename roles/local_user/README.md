# Local User Role

This is a minimal role to create a local user. This role needs root privileges, which is not available for the user we are about to create.

Once it exists, all customization can be made by running other playbooks using an SSH connection made by the target user.
